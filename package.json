{
  "name": "breakout_game_phaser",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "deploy.sh"
  },
  "devDependencies": {
    "vite": "^2.5.4",
    "vite-plugin-compression": "^0.3.5"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  }
}
